apiVersion: v1
kind: Secret
metadata:
  name: greport-secrets
  namespace: greport
  labels:
    app.kubernetes.io/name: greport
    app.kubernetes.io/part-of: greport
type: Opaque
stringData:
  github-token: "REPLACE_WITH_GITHUB_TOKEN"
  db-password: "REPLACE_WITH_DB_PASSWORD"
  database-url: "postgres://greport:REPLACE_WITH_DB_PASSWORD@postgres:5432/greport"
