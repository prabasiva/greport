# GitHub personal access token (required)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Database password (required in production)
DB_PASSWORD=change-me-in-production

# Database connection URL (used by API)
# DATABASE_URL is set in docker-compose.yml pointing to the db service

# API server settings
RUST_LOG=info,tower_http=debug
# API_HOST=0.0.0.0
# API_PORT=9423

# Rate limiting
# RATE_LIMIT_PER_MINUTE=60

# Cache settings
# CACHE_TTL_SECONDS=300

# Authentication
# REQUIRE_AUTH=false

# SLA thresholds
# SLA_RESPONSE_HOURS=24
# SLA_RESOLUTION_HOURS=168

# Dashboard API URL (baked into the Next.js build)
NEXT_PUBLIC_API_URL=http://localhost:9423
