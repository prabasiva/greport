# Multi-org environment variables for docker-compose.multi-org.yml
#
# Copy this file:  cp docker/.env.multi-org.example docker/.env.multi-org
# Then fill in your actual tokens.

# ---- Fallback token ----
# Used for any org not explicitly configured below.
GITHUB_TOKEN=ghp_your_default_fallback_token

# ---- Per-org tokens ----
# Naming convention: GREPORT_ORG_<ORG_NAME_UPPERCASE>_TOKEN
# Hyphens in org names become underscores.
#
# Example: org "my-org" -> GREPORT_ORG_MY_ORG_TOKEN

GREPORT_ORG_MY_ORG_TOKEN=ghp_my_org_fine_grained_token
GREPORT_ORG_PARTNER_ORG_TOKEN=ghp_partner_org_fine_grained_token

# ---- GitHub Enterprise org (optional) ----
# Set both TOKEN and BASE_URL for GHE orgs.
# GREPORT_ORG_ENTERPRISE_ORG_TOKEN=ghp_enterprise_token
# GREPORT_ORG_ENTERPRISE_ORG_BASE_URL=https://github.mycompany.com/api/v3

# ---- Database ----
DB_PASSWORD=change-me-in-production

# ---- Logging ----
RUST_LOG=info,tower_http=debug

# ---- Dashboard ----
NEXT_PUBLIC_API_URL=http://localhost:9423
